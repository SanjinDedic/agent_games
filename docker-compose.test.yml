# Override file for test runs.
# Usage: docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm test-runner
# Points all services at the test database so tasks enqueued by tests hit the right data.
services:
  api:
    environment:
      - DB_ENVIRONMENT=test
  worker-validation:
    environment:
      - DB_ENVIRONMENT=test
  worker-simulation:
    environment:
      - DB_ENVIRONMENT=test
  frontend:
    profiles:
      - disabled
