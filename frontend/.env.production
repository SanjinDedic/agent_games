VITE_AGENT_API_URL = https://api.agentgames.io

# Direct browser→Lambda exercise/snippet fallback (src/utils/lambdaFallback.js).
# Set to the Function URL printed by backend/fallback_lambda/deploy.sh to
# enable; leave unset to keep fallback traffic on the API-proxied route.
# VITE_EXERCISE_LAMBDA_URL = https://<id>.lambda-url.ap-southeast-2.on.aws/
